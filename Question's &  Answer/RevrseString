#include<iostream>
using namespace std;
 string revrseString( string s){
     string res;
     for(int i= s.size()-1; i>=0; i--){
        res +=s[i];
     }
     return res;
 }
  int main(){
    string s;
    getline(cin, s);
    cout<< revrseString(s);
    return 0;
  }
