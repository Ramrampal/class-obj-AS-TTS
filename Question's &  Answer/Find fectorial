//find fectorial 
#include <iostream>
using namespace std;

unsigned  long fact(int n) {
    unsigned long f = 1;
    while (n > 0) {
        f *= n--;
    }
    return f;
}

int main() {
    long num;
    cin >> num;
    cout << fact(num);
    return 0;
}
